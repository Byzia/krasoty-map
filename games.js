// Модуль управления игровым центром

function initGamesTab() {
    const container = document.getElementById('games-container');
    if (!container) return;
    
    // В будущем сюда будет подключаться логика динамической загрузки меню игр или вызова отдельного файла конкретной игры.
    console.log('Игровой центр инициализирован');
}